{
  "name": "@repo-intel/cli",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "repo-intel": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "check-types": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@repo-intel/shared": "workspace:*",
    "@repo-intel/api": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.7"
  }
}
